#include <stdio.h>

int main (void)
{
    // For i from 0 to n-1
    //     Find the smallest item between i'th item and last item
    //     Swap the smallest item with i'th item
    //
    // Big O: O(n^2)
    // Omega: Ω(n^2)
}